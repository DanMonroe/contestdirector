<form>
    <h4>{{errorMessage}}</h4>
    <p>
        {{paper-input label="First Name" value=model.firstName required=true required-errortext="First name is required."}}
    </p>
    <p>
        {{paper-input label="Last Name" value=model.lastName required=true required-errortext="Last name is required."}}
    </p>
    <p>
        {{paper-input label="Email" value=model.email}}
    </p>
    <p>
        {{#paper-button raised=true action="saveTheDangUser"}}Save{{/paper-button}}
        {{#paper-button raised=true action="cancelUser"}}Cancel{{/paper-button}}
    </p>
</form>

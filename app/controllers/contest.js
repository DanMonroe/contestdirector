import Ember from 'ember';

export default Ember.Controller.extend({
  //var controller = this;
  //currentContest: Ember.computed(function(params) {
  //  //model: function(params, transition) {
  //  debugger;
  //  var contest = this.store.find('contest', { urlpath: params.urlpath });
  //    return contest;
  //  //}
  //})
});
